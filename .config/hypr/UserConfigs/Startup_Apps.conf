$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts

$wallDIR=$HOME/Pictures/wallpapers
$lock = $scriptsDir/LockScreen.sh
$SwwwRandom = $UserScripts/WallpaperAutoChange.sh

# Plugin manager (needs reload every boot)
exec-once = hyprpm reload -n 

plugin {
    split-monitor-workspaces {
        count = 10
        keep_focused = 1
        enable_notifications = 0
        enable_persistent_workspaces = 1
    }
}

# Wallpaper stuff 
exec-once = swww-daemon --format xrgb
#exec-once = $SwwwRandom $wallDIR # random wallpaper switcher every 30 minutes 

# Startup
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = $HOME/.config/hypr/scripts/Dropterminal.sh kitty &

# Polkit (Polkit Gnome / KDE)
exec-once = $scriptsDir/Polkit.sh

# Cursor
exec-once = hyprctl setcursor Bibata-Modern-Ice 24

# Starup apps
# exec-once = hyprctl dispatch exec "[workspace 11 silent] kitty -e 'tmux-sessionizer'" &
exec-once = hyprctl dispatch exec "[workspace 11 silent] kitty" &
exec-once = hyprctl dispatch exec "[workspace 12 silent] firefox" &
# exec-once = hyprctl dispatch exec "[workspace 13 silent] postman" &
# exec-once = hyprctl dispatch exec "[workspace 1 silent] foot" &
# exec-once = hyprctl dispatch exec "[workspace 2 silent] firefox" &
# exec-once = thunar /home/maya/Downloads/ &
# exec-once = postman --use-gl=desktop &
exec-once = env LD_PRELOAD=/usr/lib/spotify-adblock.so spotify --uri=%U &
exec-once = waybar &
exec-once = nm-applet --indicator &
exec-once = swaync &
# exec-once = [workspace 2 silent] google-chrome-stable \
#   --disable-background-networking \
#   --ozone-platform=wayland \
#   --ozone-platform-hint=auto \
#   --use-gl=egl \
#   --use-vulkan=any \
#   --ignore-gpu-blocklist \
#   --enable-zero-copy \
#   --enable-gpu-rasterization \
#   --canvas-oop-rasterization \
#   --enable-features=CanvasOopRasterization,Vulkan \

# exec-once = ags
exec-once = qs -c overview & # Quickshell Overview
exec-once = blueman-applet & 
#exec-once = rog-control-center &
exec-once = $scriptsDir/Hyprsunset.sh init

# Clipboard manager
exec-once = wl-paste --type text --watch cliphist store 
exec-once = wl-paste --type image --watch cliphist store

# Starting hypridle to start hyprlock
exec-once = hypridle &

# Start pyprland daemon
exec-once = pypr &

# Here are list of features available but disabled by default
# exec-once = swww-daemon --format xrgb && swww img $HOME/Pictures/wallpapers/mecha-nostalgia.png  # persistent wallpaper

#gnome polkit for nixos
#exec-once = $scriptsDir/Polkit-NixOS.sh

# xdg-desktop-portal-hyprland (should be auto starting. However, you can force to start)
#exec-once = $scriptsDir/PortalHyprland.sh

# exec-once = qs
exec-once = $scriptsDir/KeybindsLayoutInit.sh
